server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/heartmatch
    username: root
    password: root

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

  jackson:
    serialization:
      FAIL_ON_EMPTY_BEANS: false

jwt:
  secret: heartmatch_secret_key_2026
  expiration: 86400000